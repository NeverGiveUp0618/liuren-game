# 大六壬 liuren-game ·「案例库」纵向填充 · 交接说明

## ⚠️ 最重要——工作目录（务必遵守）

- **只在这个目录里工作**：
  `/Users/xiaojin/Documents/文稿同步文件夹/03_学习 (Learning)/Seafile/学习资料/自创项目/liuren-game/`
- **绝对不要**在 `~/`（家目录根）或别处另建 clone / 另存副本！
  （历史教训：之前把 bazi-game / yangong-beisong / fengshui / zeri / 八字象义 / mingli-home 一堆项目建在了 `~/` 根目录，导致和自创项目里的副本分叉，事后花大力气才逐个合并归位。所有项目都必须待在「自创项目」目录内。）
- GitHub：`NeverGiveUp0618/liuren-game`，主分支 `main`。改完 `git push origin main`（用你环境里配好的凭据/PAT）。
- 单文件项目：所有代码/数据都在 `index.html`（约 8700+ 行）。临时验证文件放 `/private/tmp/.../scratchpad`，不要塞进项目目录。

## 任务

纵向填充**「案例库」**模块——按占类把大六壬案例**尽量收全**（区别于「入门课」的每类精选）。**当前先把「求财」收全**，之后依次求官 / 占病 / 占婚 / 失物 / 词讼 / 占宅 / 占产 …

## 两个模块别搞混（关键）

App 内「断案入门」下有两个平行模块，共用同一套渲染函数（`drMenu` / `drOpen` / `drTogSec`），靠全局变量 `_caseSrc` 切换：

| 模块 | 数据数组 | 入口 | 你动不动 |
|---|---|---|---|
| **入门课**（精选，14篇44例） | `DR_CASES` | `_caseSrc='intro'` | **绝对别动** |
| **案例库**（纵向收全） | `DR_CASELIB` | `_caseSrc='lib'` | **只往这个加数据** |

- 在 index.html 里搜 `const DR_CASELIB` 找到案例库数组（目前只有 1 条 `lcai1`）。**新案例只往 `DR_CASELIB` 数组里加**，加在 `]` 前。
- `drMenu` 会按 `_caseSrc` 自动切数据源、目录标题、按钮文案、展开记忆 key；`drOpen(id)` 会在 `DR_CASES` 和 `DR_CASELIB` 两库里找 id 并自动切 `_caseSrc`。**这些函数不用改，只填数据。**
- ⚠️ 变量名 `CASE_LIB` 已被历代课例库占用，案例库数组叫 `DR_CASELIB`，别再撞名。

## 数据结构（每条案例）

```js
{id:'lcai2',cat:'求财',name:'X日 · 标题',
 desc:'目录副标题(一句)',
 bg:'占问背景，可含 <b> 标签',
 board:{r:'日干支',yj:'月将',zs:'占时',sc:['初传','中传','末传'],mt:'原著格名·日局·机理'},
 bnote:'盘要点：三传、日干、关键位置(干上/支上/发用)，<b class="c-r">高亮关键字</b>',
 steps:[
   {h:'第①步　标题',p:'知识点(通用、白话、零基础能懂)',a:'套用到本盘'},
   {h:'第②步　…',p:'…',a:'…'},
   {h:'第③步　…',p:'…',a:'…'}
 ],
 clogic:'结论推理一句',duan:'原著断语原文',dgood:true或false(true=吉✅/false=凶⚠️),
 recap:'① … ② … ③ … ④ …(本例学到的知识点)',
 src:'飘《大六壬通解》…·X格 或 飘《图解六壬大全·毕法赋》·X格'}
```
- **id 命名**：`l` + 占类拼音 + 序号 —— 求财 `lcai2/lcai3…`、求官 `lguan1…`、占病 `lbing1…`、占婚 `lhun1…`、失物 `lshi1…`、词讼 `lsong1…`。**别和 DR_CASES 的 `cai1`/`guan1` 撞**（drOpen 靠 id 找，撞了会错）。
- 字符串用**单引号**包裹，内部只能用中文引号「」""''和全角标点，**绝不能出现半角单引号 `'`**（破坏 JS）。
- `dgood` 控制结论图标（吉✅/凶⚠️）。案例页盘下方会自动显示"九宗门课体(引擎判)＋原著格名"，无需你处理。

## 盘参数来源（务必如此，别自排盘）

**一律从毕法赋四库版 `BF_KU` 已验证课例取盘**：`grep -n "name:'…格'" index.html` 找到格，**直接复制它的 `board:{r,yj,zs,sc,mt}` 四个参数**（这些盘录入时已用引擎复现验证过）。⚠️ 有些格 `board:null`（无盘）不能用。每个格的 `yw`(原文)/`bh`(白话) 就在该格数据里，是你写讲解、取 `duan`/`src` 的依据。

## 求财待收清单（入门课已用的别重复）

入门课求财篇已用（在 DR_CASES，**别重复**）：求财大获格 / 财神空亡格 / 将生财神格 / 求财急取格 / 危中取财格。案例库已收：土将助财格(`lcai1`)。
**待收**（grep `name:'…财…格'`，取入门课没用的）：三传财太过格、进退连茹为财格、空财格、末助初财格、见财无财格、财神传墓格、支墓作财格、交车合财、财空乘元格 …（注意财空乘元格若已被失物篇用则跳过；因财致祸类已在入门课「因财致祸篇」，别重复）。

## 教学写法铁律（照入门课样板，别降级）

参考 DR_CASES 里 cai1–cai5 的写法。每步"**先讲知识点(p，通用白话)、再套本盘(a)**"，零基础能懂；每篇尽量有吉凶对照；知识点滚雪球（复用前面学的空亡/鬼/三合/生克时点一句）。

**天将生克必标五行**：贵人土·螣蛇火·朱雀火·六合木·勾陈土·青龙木·天空土·白虎金·太常土·玄武水·太阴金·天后水。
**六亲(以日干为我)**：克我者官鬼、我克者妻财、生我者父母、我生者子孙、同我者兄弟。
**五行生克**：金克木·木克土·土克水·水克火·火克金；金生水·水生木·木生火·火生土·土生金。
**三合局(整体论,别按单字)**：申子辰合水·亥卯未合木·寅午戌合火·巳酉丑合金。
**空亡**：按日所在旬，旬首前两支为空（甲子旬空戌亥、甲午旬空辰巳…）。

## 准确性铁律（六壬项目头号大坑，逐条守）

写完每个盘，**必须用 node 复现四课，核对讲解里每处"干上/支上/发用/三传"的位置和五行**。已犯过的错：①把"发用(初传)"误称"干上"（干上=第一课上神=`tp[日干寄宫]`，未必等于发用 sc[0]）；②把巳(火)说成金——其实是巳酉丑三合金局。
- 四课算法：日干寄宫 `GAN_JI`（甲寅乙辰丙戊巳丁己未庚申辛戌壬亥癸丑）；`tp[i]=ZHI[(yj_idx+(i-zs_idx+12)%12)%12]`（ZHI=子丑寅…亥）；**干上=tp[寄宫]、支上=tp[日支]、发用=sc[0]**。昼时 ZHU_SHI=卯辰巳午未申。
- 凡"据书图注入三传"的 board 只有三传可信、干支上与贵人位未必符格义，node 复现对不上就**弃盘换盘**，绝不硬写。

## 验证流程（每批，缺一不可）

1. **node 语法检查**：抽 `<script>` 内容 `new Function` 检查，JS 错误必须 = 0。
2. **node 复现每个新盘四课**，逐一核对讲解里位置/五行/空亡/三合。
3. **jsdom 渲染验证**：加载 index.html，`w.eval('_caseSrc="lib";drMenu()')` 确认案例库目录出现新案例；`w.drOpen('lcai2')` 确认盘渲染(dr-board 有三传)、讲解非空；**再 `w.eval('_caseSrc="intro";drMenu()')` + `w.drOpen('cai1')` 确认入门课没被带坏**。scratchpad 有 `vlib.js` 可参考。
4. 全过后 `git add index.html && git commit && git push origin main`。commit message 用中文、结尾加 `Co-Authored-By:` 行。**每批(一次几条)做完立即 push，不要攒着**。

## 已完成基线

- 入门课 `DR_CASES`：14 篇 44 例（求财5/求官3/占病3/占婚3/失物3/词讼3/占宅3/占产3/趋谒3/买卖3/逃亡3/谋事3/进退3/因财致祸3）——**别动**。
- 案例库 `DR_CASELIB`：求财首例 `lcai1`（土将助财·丙子），作为写法样板。
- 框架 commit `8522543`（含 `_caseSrc` 机制、双入口、`DR_CASELIB`）。

## 完成后

更新记忆（如果你有权访问）或在 commit / 交接回执里记录：案例库已收哪些类、各类几例、最新 commit。相关记忆文件：`/Users/xiaojin/.claude/projects/-Users-xiaojin/memory/project_liuren.md`（其中记有断案入门主线、DR_CASES 结构、BF_KU 盘参数反推法等，可参考但注意时效）。
